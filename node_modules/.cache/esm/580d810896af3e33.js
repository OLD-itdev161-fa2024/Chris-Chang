let mongoose,config;_96a‍.x([["default",()=>_96a‍.o]]);_96a‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_96a‍.w("config",[["default",["config"],function(v){config=v}]]);


// Get the connection string
const db = config.get('mongoURI');

// Connect to MongoDB
const connectDatabase = async () => {
    try {
        await mongoose.connect(db, {
            useUnifiedTopology: true
        });
        console.log('Connected to MongoDB');
    } catch (error) {
        _96a‍.g.console.error(error.message);

        //exit with failure code
        process.exit(1);
    }
};

_96a‍.d(connectDatabase);