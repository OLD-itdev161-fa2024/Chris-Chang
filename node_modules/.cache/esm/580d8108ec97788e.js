let mongoose,config;_ec9‍.x([["default",()=>_ec9‍.o]]);_ec9‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_ec9‍.w("config",[["default",["config"],function(v){config=v}]]);


// Get the connection string
const db = config.get('mongoURI');

// Connect to MongoDB
const connectDatabase = async () => {
    try {
        await mongoose.connect(db, {
            useUnifiedTopology: true
        });
        console.log('Connected to MongoDB');
    } catch (error) {
        _ec9‍.g.console.error(error.message);

        //exit with failure code
        process.exit(1);
    }
};

_ec9‍.d(connectDatabase);